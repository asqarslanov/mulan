cmd-gen:
  log: "Generated {target} bindings in `{path}`"

errors:
  config:
    parse:
      invalid-type:
        message: |-
          invalid type
            key `{key}`
            is expected to be `{expected}`
            but actually has type `{actual}`
      unknown-variant:
        message: |-
          unknown variant
            key `{key}`
            is{actual}
            but is expected to be one of: {expected}
      unknown-field:
        message: "unknown field: `{actual}`"

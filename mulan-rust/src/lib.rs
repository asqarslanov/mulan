pub mod mulan;

/*
mulan::context(&["Hello", "world", 23])..

mulan::new_context(OWNED);
mulan::new_context_with(OWNED, mulan::Options::default());
mulan::context::new()

mulan::context(&..).unwrap()

mulan::context(..).t().greetings.hello_world;
mulan::context(..).t().greetings.hello("John");
mulan::context(..).current_locale();
mulan::context(..).set_locale(mulan::Locale::RuRu);
mulan::context(..).options();
mulan::context(..).set_options({ mulan::Options::default() })

assert_eq!(mulan::t() mulan::context(&[]).t());
assert_eq!(mulan::current_locale() mulan::context(&[]).current_locale());
assert_eq!(mulan::.. mulan::context(&[])..);
*/
